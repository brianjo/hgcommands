``` 
hg isl

launch Interactive Smartlog web server on localhost

    Interactive Smartlog (ISL) is a GUI that facilitates source control
    operations, such as creating, reordering, or rebasing commits. Running
    this command launches a web server that makes ISL available via a web
    interface.

Options:

-p --port VALUE port for ISL web server (default: 3011) --open open ISL
in a local browser (default: True)

(some details hidden, use --verbose to show complete help)

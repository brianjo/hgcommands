``` 
hg tag \[-f\] \[-l\] \[-m TEXT\] \[-d DATE\] \[-u USER\] \[-r REV\]
NAME...

add one or more tags for the current or given revision

    This command is deprecated.

Options:

-f --force force tag -l --local make the tag local -r --rev REV revision
to tag --remove remove a tag -e --edit invoke editor on commit messages
-m --message TEXT use text as commit message -d --date DATE record the
specified date as commit date -u --user USER record the specified user
as committer

(some details hidden, use --verbose to show complete help)
